@{
    ViewData["Title"] = "Reset Password - TownTrek";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Scripts {
}

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h1>Reset Your Password</h1>
            <p>Enter your email address and we'll send you a link to reset your password</p>
        </div>

        <form class="auth-form" id="forgotPasswordForm" method="post" asp-action="ForgotPassword">
            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    class="form-input" 
                    placeholder="your@email.com"
                    required
                    autocomplete="email"
                />
            </div>

            <button type="submit" class="auth-btn auth-btn-primary">
                Send Reset Link
            </button>
        </form>

        <div class="auth-links">
            <p style="color: var(--dark-gray); margin-bottom: var(--space-md);">
                Remember your password? 
                <a asp-area="" asp-controller="Auth" asp-action="Login" class="auth-link">Sign in here</a>
            </p>
            <p>
                Don't have an account? 
                <a asp-area="" asp-controller="Auth" asp-action="Register" class="auth-link">Join TownTrek today</a>
            </p>
        </div>
    </div>
</div>

<!-- Help Section -->
<section style="background-color: var(--light-gray); padding: var(--space-2xl) 0;">
    <div class="container">
        <div class="text-center">
            <h2 style="color: var(--charcoal); margin-bottom: var(--space-md);">
                Need Help?
            </h2>
            <p style="color: var(--dark-gray); font-size: var(--body-large); margin-bottom: var(--space-lg);">
                Our support team is here to help you get back into your account
            </p>
            
            <div class="grid grid-2" style="max-width: 600px; margin: 0 auto;">
                <div class="text-center">
                    <div style="width: 50px; height: 50px; background-color: var(--carolina-blue); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; margin: 0 auto var(--space-md);">
                        <span style="font-size: 20px;">📧</span>
                    </div>
                    <h3 style="color: var(--charcoal); font-size: var(--h6-size); margin-bottom: var(--space-sm);">
                        Email Support
                    </h3>
                    <p style="color: var(--dark-gray); font-size: var(--body-small);">
                        support@TownTrek.co.za
                    </p>
                </div>

                <div class="text-center">
                    <div style="width: 50px; height: 50px; background-color: var(--hunyadi-yellow); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; margin: 0 auto var(--space-md);">
                        <span style="font-size: 20px;">📞</span>
                    </div>
                    <h3 style="color: var(--charcoal); font-size: var(--h6-size); margin-bottom: var(--space-sm);">
                        Phone Support
                    </h3>
                    <p style="color: var(--dark-gray); font-size: var(--body-small);">
                        +27 21 123 4567
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>